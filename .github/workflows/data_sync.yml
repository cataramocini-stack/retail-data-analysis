name: Market Volatility Analysis Pipeline

on:
  schedule:
    # Executa a cada 20 minutos
    - cron: "*/20 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11" # Versão estável para automação com Playwright Stealth

      - name: 📦 Instalar dependências
        run: |
          python -m pip install --upgrade pip
          
          # FORÇA a instalação de uma versão do setuptools que contém o pkg_resources
          # Usamos a versão 69 para garantir compatibilidade total
          pip install "setuptools<70.0.0"
          
          # Instala as demais dependências do projeto
          pip install -r requirements.txt
          
          # Instala o navegador Chromium necessário para o scraping
          playwright install chromium
          playwright install-deps chromium

      - name: 🔍 Validar ambiente
        run: |
          # Teste rápido: Se falhar aqui, o log nos dirá exatamente o porquê
          python -c "import pkg_resources; print('✅ Módulo pkg_resources carregado com sucesso!')"

      - name: 🚀 Execute Stochastic Price Polling
        env:
          INGESTION_ENDPOINT_PRIMARY: ${{ secrets.INGESTION_ENDPOINT_PRIMARY }}
          AFFILIATION_DATA_METRIC: ${{ secrets.AFFILIATION_DATA_METRIC }}
        run: python market_regressor_engine.py

      - name: 📸 Salvar screenshot de debug
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshot
          path: ./debug.png
          retention-days: 3
          if-no-files-found: ignore

      - name: 🔄 Commit e Push automático
        run: |
          git config user.name "Market Regressor Bot"
          git config user.email "bot@market
